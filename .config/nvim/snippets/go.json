{
  "if err != nil block": {
    "prefix": "ifr",
    "body": [
      "if err != nil {",
      "\t$0",
      "}"
    ],
    "description": "Basic if err != nil statement"
  },
  "if err := func(); err != nil block": {
    "prefix": "ifr2",
    "body": [
      "if err := ${1}; err != nil {",
      "\t$0",
      "}"
    ],
    "description": "Scoped err declaration; err != nil statement"
  },
  "if !reflect.DeepEqual block": {
    "prefix": "ifde",
    "body": [
      "if !reflect.DeepEqual(${1:got}, ${2:want}) {",
      "\tt.Errorf(\"got %v; want %v\", ${1:got}, ${2:want})",
      "}"
    ],
    "description": "Check DeepEqual and report got vs want"
  }
}
